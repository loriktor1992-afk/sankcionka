# 🚗 Парсер idubid.com → Санкционка.рф

Автоматический парсер, который собирает автомобили с idubid.com и добавляет их на твой сайт.

## 📋 Что делает парсер:

1. ✅ Заходит на idubid.com (нужно войти вручную)
2. ✅ Парсит все карточки авто со страниц списка
3. ✅ Заходит в детальные страницы каждого авто
4. ✅ Скачивает все фото (до 20 на машину)
5. ✅ Извлекает характеристики (марка, модель, год, пробег, топливо, КПП, привод)
6. ✅ Автоматически добавляет через API на твой сайт
7. ✅ Удаляет временные файлы

## 🛠️ Установка

### 1. Установи зависимости Python:

```bash
pip install selenium beautifulsoup4 requests
```

### 2. Скачай ChromeDriver:

- Скачай с https://chromedriver.chromium.org/
- Или через `pip install chromedriver-autoinstaller`
- Положи в PATH или в папку с проектом

## 🚀 Запуск

### Шаг 1: Запусти сайт

```bash
npm run dev
```

Сайт должен работать на http://localhost:3000

### Шаг 2: Запусти парсер

```bash
python parser_idubid.py
```

### Шаг 3: Авторизуйся на idubid.com

1. Откроется браузер Chrome
2. Введи логин и пароль на idubid.com
3. Перейди на страницу с нужным брендом (например, Toyota)
4. Когда увидишь список машин с кнопкой "Next" — вернись в консоль
5. Нажми Enter

### Шаг 4: Парсер работает автоматически

- Проходит по всем страницам списка
- Заходит в каждое объявление
- Скачивает фото
- Добавляет на сайт
- Показывает прогресс в консоли

## ⚙️ Настройки

Открой `parser_idubid.py` и измени константы в начале файла:

```python
API_URL = "http://localhost:3000/api/admin/add-car"  # URL API
MAX_CARS = 1500  # Максимум машин
MAX_PHOTOS_PER_CAR = 20  # Максимум фото на машину
```

Для запуска без окна браузера (фоновый режим):

```python
options.add_argument("--headless")  # Раскомментируй эту строку
```

## 📊 Что парсится:

### Автоматически извлекается:
- ✅ Марка (первое слово из названия)
- ✅ Модель (остальное название)
- ✅ Год (находит 4 цифры типа 2015, 2020)
- ✅ Пробег (находит "123,456 km" и преобразует)
- ✅ Топливо (hybrid, electric, diesel, petrol)
- ✅ КПП (CVT, automatic, manual)
- ✅ Привод (AWD, FWD, RWD)
- ✅ Аукционная оценка (Grade 4.5, Grade R)
- ✅ Описание (полный текст со страницы)
- ✅ Все фото (до 20 штук)

## 🎯 Результат

После завершения парсинга:
- Все машины будут на сайте
- Фото сохранены в `public/images/models/[brand]/`
- Данные в `data/models/[brand].json`
- Временные файлы удалены

## ⚠️ Важно:

1. **Сайт должен быть запущен** (`npm run dev`)
2. **Нужна авторизация** на idubid.com
3. **Хорошее соединение** (парсер скачивает много фото)
4. **Терпение** - для 1500 машин нужно несколько часов

## 🐛 Проблемы?

### Ошибка: "API не отвечает"
- Проверь, что сайт запущен на localhost:3000
- Проверь `API_URL` в скрипте

### Фото не скачиваются
- Проверь интернет
- Некоторые фото могут быть защищены - это нормально

### Парсер не находит карточки
- Сайт изменил вёрстку
- Проверь Inspect Element на idubid.com
- Обнови селекторы в коде

## 📈 Прогресс в консоли

```
🚗 [1/20] Обрабатываю карточку...
📋 Toyota Prius 2020
🔗 https://www.idubid.com/car/12345
📸 Найдено фото: 15
  ✓ Скачано фото 1/15
  ✓ Скачано фото 2/15
  ...
🏷️  Марка: toyota | Модель: Prius 2020
✅ Успешно добавлено: toyota Prius 2020
✨ Добавлено машин: 1/1500
```

Удачи! 🚀
